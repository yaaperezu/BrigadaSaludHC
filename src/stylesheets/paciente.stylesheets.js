import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'flex-start',
        marginTop: 30
    },
    fab: {
        position: 'absolute',
        margin: 10,
        right: 0,
        bottom: 0
      }
});